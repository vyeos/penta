# Penta development environment.
# Local dev PostgreSQL (see docker/docker-compose.yml).
PENTA_DEV_PG_URL=postgres://penta:penta@localhost:55432/penta_dev

# App database location (SQLite). Defaults to the OS app-data dir if unset.
# PENTA_APP_DB=~/.penta/penta.db
